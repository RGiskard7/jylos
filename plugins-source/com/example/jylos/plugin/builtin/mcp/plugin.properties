# Multi-file plugin bundle descriptor (see scripts/build-plugins.sh).
# Every .java under this directory is compiled into a single JAR; every .jar under
# lib/ is a third-party dependency merged into that same JAR.
plugin.class=com.example.jylos.plugin.builtin.mcp.McpServerPlugin
plugin.jar=McpServerPlugin
